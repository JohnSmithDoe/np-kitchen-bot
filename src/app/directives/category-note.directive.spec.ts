import { CategoryNoteDirective } from './category-note.directive';

describe('CategoryNoteDirective', () => {
  it('should create an instance', () => {
    const directive = new CategoryNoteDirective();
    expect(directive).toBeTruthy();
  });
});
